# git-remote-tasks runs on the Python standard library alone.
# The single optional dependency is only needed for MS To Do
# device-code OAuth; import is guarded at runtime.

msal>=1.24 ; extra == "mstodo"
